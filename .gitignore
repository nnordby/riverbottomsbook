.DS_Store
._*
.localized

# Local / private working files — never publish to this public repo
# billing ledger
/TIME_LOG.md
# internal admin tooling (issues.php)
/admin/
# feedback backend: the safe example config IS tracked (feedback-config.example.php).
# Keep the internal runbook, the duplicate script (real one is /feedback.php), and
# any real config out of this PUBLIC repo / live webroot.
/feedback/DEPLOY.md
/feedback/feedback.php
/feedback/feedback-config.php
# editor/agent local settings
/.claude/

# Stray source assets superseded by the committed .webp files
/img/portrait_chrs_perales.jpeg
/img/portrait_leo_hellyer.jpeg
/img/webp/
